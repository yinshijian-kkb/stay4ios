C语言中的运算符

c 语言中的优先级：
共有15级，1级最高，15级最低。例如： int a  = b + c;
http://www.slyar.com/blog/c-operator-priority.html（C语言运算符优先级）
结合性：前提是运算符的优先级是相同的。分为左结合、右结合。

写C语言中的运算符的时候，需要考虑结合性和优先级。

逗号运算符和逗号表达式：
用逗号连接的表达式叫做逗号表达式，逗号表达式的值是最后一个表达式的值。例如： result = (a+1,b+2,c+3,10)  == > result = 10;

三目运算符的右结合理解：
例如：a>b?0:c<d?3:90【先计算c<d?3:90 所以，是从右边开始的】


